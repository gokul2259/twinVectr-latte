const defaultState = {
  recentWorks: [],
}

function recentWorksAction(state = defaultState, action) {
  switch(action.type) {
    default:
    return state;
  }
}

module.exports = {recentWorksAction};
